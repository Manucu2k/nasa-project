name: NASA Project CI
on: 
  push: 
    branches: [ main ]
  pull_request: 
    branches: [ main ]
jobs:
  env:
    CI: true
  strategy:
    matrix: 
      node-version: [ 14.x, 16.x]
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - name: Use Node.js version ${{ matrixc.node-version }}
        uses: actions/setup-node@v3 
        with: 
          node-version: ${{ matrixc.node-version }}
          cache: 'npm'
      - run: npm install
      - run: npm run build --prefix client
      - run: npm test
